<!--pages/teacher/teacher.wxml-->

<block wx:for="{{menus}}">
  <view class='teacher'>
    <image src="{{item.url}}" style='' mode='widthFix' class="swiper-image" />{{item.name}} : {{item.msg}}
  </view> 
</block>